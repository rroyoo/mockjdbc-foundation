<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.rroyoo.mockjdbc</groupId>
    <artifactId>mockjdbc</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <modules>
        <module>mockjdbc-proxy</module>
        <module>mockjdbc-proto</module>
        <module>mockjdbc-mock</module>
        <module>mockjdbc-wiremock</module>
    </modules>

    <name>MockJDBC Ecosystem Parent</name>
    <description>Parent POM for MockJDBC ecosystem with submodules.</description>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <maven.plugin.version.compiler>3.11.0</maven.plugin.version.compiler>
        <maven.plugin.version.os-maven>1.7.1</maven.plugin.version.os-maven>
        <maven.plugin.version.protobuf>5.0.0</maven.plugin.version.protobuf>
        <maven.version.byte-buddy>1.18.3</maven.version.byte-buddy>
        <maven.version.grpc>1.79.0</maven.version.grpc>
        <maven.version.javax-annotation-api>1.3.2</maven.version.javax-annotation-api>
        <maven.version.kafka>3.8.1</maven.version.kafka>
        <maven.version.mockito>5.12.0</maven.version.mockito>
        <maven.version.protobuf>4.34.0</maven.version.protobuf>
        <maven.version.test.junit-jupiter>5.11.3</maven.version.test.junit-jupiter>
        <maven.version.testcontainers>1.20.4</maven.version.testcontainers>
        <maven.version.wiremock>3.9.1</maven.version.wiremock>
        <revision>1.0.0-SNAPSHOT</revision>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-bom</artifactId>
                <version>${maven.version.protobuf}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-bom</artifactId>
                <version>${maven.version.grpc}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy-parent</artifactId>
                <version>${maven.version.byte-buddy}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>javax.annotation</groupId>
                <artifactId>javax.annotation-api</artifactId>
                <version>${maven.version.javax-annotation-api}</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-clients</artifactId>
                <version>${maven.version.kafka}</version>
            </dependency>

            <dependency>
                <groupId>org.wiremock</groupId>
                <artifactId>wiremock</artifactId>
                <version>${maven.version.wiremock}</version>
            </dependency>

            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${maven.version.testcontainers}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${maven.version.test.junit-jupiter}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${maven.version.mockito}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>kr.motd.maven</groupId>
                    <artifactId>os-maven-plugin</artifactId>
                    <version>${maven.plugin.version.os-maven}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.plugin.version.compiler}</version>
                    <configuration>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>io.github.ascopes</groupId>
                    <artifactId>protobuf-maven-plugin</artifactId>
                    <version>${maven.plugin.version.protobuf}</version>
                    <configuration>
                        <protoc>${maven.version.protobuf}</protoc>
                        <plugins>
                            <plugin kind="binary-maven">
                                <groupId>io.grpc</groupId>
                                <artifactId>protoc-gen-grpc-java</artifactId>
                                <version>${maven.version.grpc}</version>
                            </plugin>
                        </plugins>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>generate</goal>
                            </goals>
                            <phase>
                                generate-sources
                            </phase>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
